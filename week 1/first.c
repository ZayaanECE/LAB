#include<stdio.h>
int main()
{
	int x,y;
        printf("Enter 2 values ");
	scanf("%d %d",&x,&y);
	printf("the sum of the 2 values are %d",x+y);
	printf("\n the difference of the 2 values are %d",x-y);
	printf("\n the product of the 2 values are %d",x*y);
	printf("\n the quotient of the 2 values are %d",x/y);
	printf("\n the remainder of the 2 values are %d",x%y);
	return 0;
}
